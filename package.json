{
  "name": "liquid",
  "version": "0.1.0",
  "description": "Monorepo for Liquid",
  "author": "Adam Jedlička",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "liquid dev",
    "build": "liquid build",
    "serve": "liquid serve",
    "lint": "eslint --ext .js --ignore-path .gitignore .",
    "test": "jest"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "liquid-js": "*",
    "solid-js": "*",
    "solid-meta": "*"
  },
  "devDependencies": {
    "eslint": "^7.14",
    "eslint-config-prettier": "^7.1",
    "eslint-plugin-prettier": "^3.1",
    "jest": "^26.6",
    "prettier": "^2.2"
  }
}
