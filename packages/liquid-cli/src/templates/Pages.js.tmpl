import { Suspense } from 'solid-js'

<%_ for (const page of pages) { _%>
import <%- page.name -%> from '<%- page.component -%>'
<%_ } _%>

export default () => {
  return (
    <Suspense>
      <_404 />
    </Suspense>
  )
}
