import { Suspense } from 'solid-js'

<%_ for (const layout of layouts) { _%>
import _<%- layout.ident -%> from '<%- layout.component -%>'
<%_ } _%>

export default (props) => {
  return (
    <Suspense>
      <_default>{props.children}</_default>
    </Suspense>
  )
}
